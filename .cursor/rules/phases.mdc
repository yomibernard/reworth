---
description: ReWorth phase plan and git tags — execute one phase at a time
alwaysApply: true
---

# ReWorth — phase rules

**Current phase: 2.4 (Verticals & Commercial)** — next tag `v1.3`.

Do **not** implement features from later post-launch prompts in the same PR.

## MVP tags

`v0.0-scaffold` → … → `v0.9.0-rc`

## Post-launch

`v1.0-swap` · `v1.1-moving-communities` · `v1.2-intelligence` (done) · `v1.3` Verticals · `v2.0` AI · `v2.1` B2B

## Resume

See `docs/PHASE_STATUS.md`. Prompt package: `CURSOR-PROMPT-PHASE2-3.md`.
