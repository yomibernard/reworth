---
description: ReWorth phase plan and git tags — execute one phase at a time
alwaysApply: true
---

# ReWorth — phase rules

**Post-launch series complete** — last tag `v2.1-expansion` (Phase 3.2).

Do **not** invent a new phase without an explicit product prompt.

## MVP tags

`v0.0-scaffold` → … → `v0.9.0-rc`

## Post-launch

`v1.0-swap` · `v1.1-moving-communities` · `v1.2-intelligence` · `v1.3-verticals` · `v2.0-ai-assistant` · `v2.1-expansion` (done)

## Resume

See `docs/PHASE_STATUS.md`. Next: city launches via `docs/CITY_PLAYBOOK.md` (config-only).
