---
description: ReWorth phase plan and git tags — execute one phase at a time
alwaysApply: true
---

# ReWorth — phase rules

**Current phase: 3.2 (B2B & Expansion)** — next tag `v2.1`.

Do **not** implement features from later prompts in the same PR.

## MVP tags

`v0.0-scaffold` → … → `v0.9.0-rc`

## Post-launch

`v1.0-swap` · `v1.1-moving-communities` · `v1.2-intelligence` · `v1.3-verticals` · `v2.0-ai-assistant` (done) · `v2.1` B2B

## Resume

See `docs/PHASE_STATUS.md`. Prompt package: `CURSOR-PROMPT-PHASE2-3.md`.
