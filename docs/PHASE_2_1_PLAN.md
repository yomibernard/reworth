# Phase 2.1 plan — Swap & Give-Away (`v1.0-swap`)

**Roles:** Backend Lead · Payments · Frontend · Mobile · QA · Solution Architect

## Deliverables
1. Schema: SwapProposal, GiveawayClaim, Order.transactionType + dual legs
2. ADR-003 — failed/returned swap leg → support + reversal
3. API + expiry jobs + trust hooks
4. Web/mobile SWAP + claim UX
5. Tests + tag `v1.0-swap`
